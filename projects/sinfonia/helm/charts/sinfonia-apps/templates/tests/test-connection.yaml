apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sinfonia-apps.fullname" . }}-test-connection"
  labels:
    {{- include "sinfonia-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sinfonia-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
