apiVersion: v1
kind: Service
metadata:
  name: {{ include "sinfonia-apps.fullname" . }}
  labels:
    {{- include "sinfonia-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sinfonia-apps.selectorLabels" . | nindent 4 }}
