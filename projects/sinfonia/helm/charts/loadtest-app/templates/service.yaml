apiVersion: v1
kind: Service
metadata:
  name: {{ include "loadtest-app.fullname" . }}
  labels:
    {{- include "loadtest-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: 30080
      protocol: TCP
      name: http
  selector:
    {{- include "loadtest-app.selectorLabels" . | nindent 4 }}
