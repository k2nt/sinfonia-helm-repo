apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "loadtest-appclear.fullname" . }}-test-connection"
  labels:
    {{- include "loadtest-appclear.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "loadtest-appclear.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
