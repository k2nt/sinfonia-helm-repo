apiVersion: v1
kind: Service
metadata:
  name: {{ include "loadtest-appclear.fullname" . }}
  labels:
    {{- include "loadtest-appclear.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loadtest-appclear.selectorLabels" . | nindent 4 }}
